## 提示

从输入读取 $s_0$ 和 $n$ 之后，定义变量 $count$ 代表与 $s_0$ 相同的诗句数量。

使用`for`和`range`搭配循环 $n$ 次，每次输入一个字符串，存储至变量 $s$。然后使用`if`判断 $s=s_0$（`s==s0`）是否成立，若成立，则使 $count+1$（`count = count + 1`或`count += 1`）。最终，循环结束后输出 $count$，即为答案。